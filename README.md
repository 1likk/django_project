### Важные вещи. # Общая картина.

### Всем привет, это мой первый проект на фреймворке django. 🥵

# 07.10.2025 Я настроил базовые конфиги проекта.

# 08.10.2025 MVT pattern Module-отвечает за базу данных, views-отвечает за логику самого application и templates-отвечает за то что видит конечный пользователь, создал простую базу данных на основа sqlite. 🤤

### Sqlite -> main commands: 1. manage.py makemigrations проверка на корректность. 2. manage.py migrate запускает.

### admin panel -> creating superuser: python manage.py createsuperuser .

# 13.10.2025 If statement's context's in HTML -> {% if products %} -> {% for p in products %} -> {{ p.name }}, {{ p.description }} -> {% endfor %} -> {% else %} -> {% endif %}.

### методы all(), get(), filter().

# 14.10.2025 HTTP requests: GET, POST, DELETE, PUT, PATCH, HEAD, OPTIONS.

### HTTP requests: "get"= не конфиденциально можно использовать например 'фильтрация' -> "post"= конфиденциально можно использовать например 'log in', 'register'.

# 15.10.2025 Django forms 
# 15.10.2025-18.10.2025 Подучил css стили чтобы выглядело красивенько.

# 20.10.2025 Detail html работа с медиа файлами.

# 25.10.2025 В целом логика сайта django top-чик.

# 30.10.2025 login, register оброботка POST, GET запросов.

# 02.11.2025 ВСЕ КОНЕЕЦ
